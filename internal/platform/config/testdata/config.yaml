http:
  port: 8080
ai:
  workflow:
    retrievalLimit: 6
    rewritePrompt: "请把问题改写成适合检索的中文查询。"
    toolParamPrompt: "请提取工具参数并输出 JSON。"
    knowledgePrompt: "请基于知识上下文回答。"
database:
  driver: "sqlite"
  dsn: "data/agentrag.db"
